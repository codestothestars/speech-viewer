import { Component, h } from '@stencil/core';

/**
 * The home page.
 */
@Component({
  styleUrl: 'home.css',
  tag: 'app-home'
})
export class Home {
  /**
   * Renders the home page.
   *
   * @returns The rendered home page.
   */
  public render = (): unknown => <div />;
}
