/**
 * Basic information about an author's speech, in a context in which the author
 * is already known.
 */
export interface AuthorSpeechInfo {
  /**
   * The speech's identifier.
   */
  id: number;

  /**
   * The speech's name.
   */
  name: string;
}
